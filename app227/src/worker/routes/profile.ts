import type { Env } from "../../shared/types";
import { notImplemented } from "./_stub";

/* /api/profile — stub. */
export async function profile(_request: Request, _env: Env): Promise<Response> {
  return notImplemented("profile");
}
