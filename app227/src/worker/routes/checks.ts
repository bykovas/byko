import type { Env } from "../../shared/types";
import { notImplemented } from "./_stub";

/* /api/checks — stub. */
export async function checks(_request: Request, _env: Env): Promise<Response> {
  return notImplemented("checks");
}
