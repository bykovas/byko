import type { Env } from "../../shared/types";
import { notImplemented } from "./_stub";

/* /api/advance — stub. */
export async function advance(_request: Request, _env: Env): Promise<Response> {
  return notImplemented("advance");
}
