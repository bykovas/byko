/* screen: start — stub, no markup beyond its own name, no styles, no SDK. */
export function start(): HTMLElement {
  const el = document.createElement("main");
  el.textContent = "start — not implemented";
  return el;
}
