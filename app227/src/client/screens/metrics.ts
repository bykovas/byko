/* screen: metrics — stub, no markup beyond its own name, no styles, no SDK. */
export function metrics(): HTMLElement {
  const el = document.createElement("main");
  el.textContent = "metrics — not implemented";
  return el;
}
