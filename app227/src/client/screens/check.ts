/* screen: check — stub, no markup beyond its own name, no styles, no SDK. */
export function check(): HTMLElement {
  const el = document.createElement("main");
  el.textContent = "check — not implemented";
  return el;
}
