/* screen: auth — stub, no markup beyond its own name, no styles, no SDK. */
export function auth(): HTMLElement {
  const el = document.createElement("main");
  el.textContent = "auth — not implemented";
  return el;
}
