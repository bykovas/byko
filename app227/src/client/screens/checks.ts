/* screen: checks — stub, no markup beyond its own name, no styles, no SDK. */
export function checks(): HTMLElement {
  const el = document.createElement("main");
  el.textContent = "checks — not implemented";
  return el;
}
