/* screen: profile — stub, no markup beyond its own name, no styles, no SDK. */
export function profile(): HTMLElement {
  const el = document.createElement("main");
  el.textContent = "profile — not implemented";
  return el;
}
