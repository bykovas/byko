/* screen: result — stub, no markup beyond its own name, no styles, no SDK. */
export function result(): HTMLElement {
  const el = document.createElement("main");
  el.textContent = "result — not implemented";
  return el;
}
